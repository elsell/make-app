package __DOMAIN__

import (
	"strings"
	"time"
)

type ID string
type Attributes struct {
__DOMAIN_GO_FIELDS__}
type Entity struct {
	ID          ID
	OwnerUserID string
	Attributes
	CreatedAt, UpdatedAt time.Time
}

func New(id ID, ownerUserID string, attributes Attributes) (Entity, error) {
__DOMAIN_STRING_VALIDATION__	if id == "" || strings.TrimSpace(ownerUserID) == "" {
		return Entity{}, ErrInvalidFields
	}
	return Entity{ID: id, OwnerUserID: ownerUserID, Attributes: attributes}, nil
}
