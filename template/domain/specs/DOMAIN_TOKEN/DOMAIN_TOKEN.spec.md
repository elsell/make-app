# __DOMAIN__ Domain Specification

`__DOMAIN__` is initially a user-owned resource with an immutable ID and required
name. Its owner is established at creation. The owner may view, update, and delete
it. Lists are scoped by authenticated owner in persistence; individual reads and
mutations require SpiceDB permission checks and hide unauthorized identifiers.

This generated domain is a starting point. Update this specification before
adding behavior, fields, sharing, routes, persistence, or authorization relations.

